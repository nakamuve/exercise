// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Static site — output is plain HTML+assets, deployable to Cloudflare Pages
  // (or any static host) without a runtime. Set `site` when you know the
  // production URL so sitemap/canonical links resolve:
  //   site: 'https://your-subdomain.pages.dev',
});
